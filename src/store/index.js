import { createStore } from 'vuex'
// import env from '@/env.js'
export default createStore({
  state: {
    // domain: env.domain,
    is_active: null,
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
